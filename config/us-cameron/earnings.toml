# 決算発表日のブラックアウト表。ross_cameron の blackout_file（既定では未使用） から参照。
#
# 日足運用では決算ギャップを避けられないため、発表 N 日前〜当日は新規建てせず、
# 保有中なら手仕舞う（exit_before_earnings）。
#
# **手動で更新すること。** 各社の IR ページか、証券会社の決算カレンダーで
# 確認する。空のままでもエラーにはならないが、その場合ブラックアウトは効かない。
#
# 形式: TICKER = ["YYYY-MM-DD", ...]

[earnings]
# AAPL = ["2026-10-29"]
# MSFT = ["2026-10-27"]
